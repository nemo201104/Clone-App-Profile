#!/system/bin/sh
umask 077
MODDIR=$(CDPATH= cd -- "${0%/*}/.." && pwd) || exit 1
export CLASSPATH="$MODDIR/lib/core.jar"
exec /system/bin/app_process /system/bin io.github.nemo.cap.Core "$MODDIR" "$@"
